# `types/` — Definiciones de tipos

TYPE / STRUCT / ENUM compartidos por el proyecto.

Contenido previsto:
- `PumpProfile` — perfil parametrizado de bomba modulante (ver M09).
- Enums `ST_*` con nombre legible (estados de FB_Solar, FB_ClimateReversible, etc.).
